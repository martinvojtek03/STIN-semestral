name: .NET Core CI

on:
  push:
    branches: [ main, Develop, 'feat/**' ]
  pull_request:
    branches: [ main, Develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore

    - name: Test with Coverage
      # Pøidali jsme --results-directory, aby ReportGenerator vìdìl, kde pøesnì hledat
      run: >
        dotnet test --no-build --verbosity normal 
        --collect:"XPlat Code Coverage" 
        --results-directory ./TestResults
        -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Exclude="[Stin-Semestral]Program,[*]AspNetCoreGeneratedDocument.*,[*]Stin_Semestral.Migrations.*,[*]Stin_Semestral.Data.*,[*]Stin_Semestral.View.*,[*]Stin_Semestral.Views.*"

    - name: Install ReportGenerator
      run: dotnet tool install -g dotnet-reportgenerator-globaltool

    - name: Generate Report
      # Zmìnili jsme cestu na ./TestResults, aby to bylo v souladu s testy
      run: |
        reportgenerator -reports:"./TestResults/**/*.xml" -targetdir:"coveragereport" -reporttypes:"MarkdownSummary;HtmlInline_AzurePipelines"

    - name: Upload coverage summary
      # Pøidali jsme kontrolu existence souboru, aby step neselhal potichu
      run: |
        if [ -f coveragereport/Summary.md ]; then
          cat coveragereport/Summary.md >> $GITHUB_STEP_SUMMARY
        else
          echo "## Error: Coverage Summary not found!" >> $GITHUB_STEP_SUMMARY
          exit 1
        fi

    - name: Check Coverage Threshold
      shell: pwsh
      run: |
        if (Test-Path "coveragereport/Summary.md") {
            $summary = Get-Content "coveragereport/Summary.md" -Raw
            
            # Najdeme Branch coverage
            if ($summary -match 'Branch coverage: (\d+\.?\d*)%') {
                $percent = [double]$matches[1]
                Write-Host "Current Branch Coverage: $percent%"
                if ($percent -lt 80) {
                    Write-Error "Branch coverage is below 80%! Current: $percent%"
                    exit 1
                }
            } else {
                Write-Error "Could not find branch coverage percentage in summary."
                exit 1
            }
        } else {
            Write-Error "Summary file missing."
            exit 1
        }